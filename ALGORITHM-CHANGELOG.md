# 算法修改记录

这份文件只回答一个问题：**准确率变了，是因子调整起了作用，还是比较对象变了？**

## 怎么用

1. 每条预测记录都带 `algoVersion`，取值来自 `js/signals.js` 的 `SignalGenerator.ALGORITHM_VERSION`。
2. 在表格里按「算法版本」分组对比准确率 —— 这才是因子调整的效果。
   不分组的总体准确率没有意义：它把不同算法的结果混在一起算平均。
3. 改算法的同时必须做两件事：**升 `ALGORITHM_VERSION`**，并**在下面登记一条**。

记录由后端自动维护成表格：`server/data/predictions.csv`，
也可通过 `GET /api/predictions.csv` 下载（需登录）。

## 什么改动必须登记

会影响**信号结果**或**复盘口径**的改动：

- `js/signals.js`：阈值、评分、操作建议逻辑
- `js/app.js`：五因子权重、缺失因子的剔除与重新归一、费率闸门、灵敏度档位权重
- `js/prediction.js`：判定阈值（0.3% / 2%）、复盘窗口、判定规则

**不需要**登记的：界面样式、文案、网络超时、持久化与同步 —— 这些不影响信号结果，
也不改变历史记录的含义。

## 版本表

| 版本 | 日期 | 变更要点 |
|---|---|---|
| 1.0.1 | 2026-09-23 | 仓位提示改为按 signalType 分流，不再写死阈值 |
| 1.0.0 | 2026-09-23 | 版本号起点。基线即当前算法，此后改动相对它比较 |

## 1.0.1（2026-09-23）

- **位置**：`js/signals.js` `generateActionTips`（基于综合评分的仓位建议那一段）
- **问题**：这一段拿 `totalScore` 与**写死的** 70 / 58 / 42 / 30 比较，而信号标签用的是
  灵敏度档位的阈值（保守 74/63/37/26、均衡 70/58/42/30、灵敏 66/54/46/34）。两者只在
  均衡档下重合，另外两档就会互相矛盾：
  - 保守档 61 分：标签是「观望」，提示却说「仓位30-50%」
  - 灵敏档 56 分：标签是「买入」，提示却说「等待更明确的信号」
- **改法**：改为按 `signalType` 分流。`signalType` 就是按当时档位算出来的结论，
  用它分流后，仓位提示与信号标签在结构上不可能不一致
- **影响**：只改变「哪句仓位提示在什么分值时出现」。五条提示的文案、图标、优先级都
  没动；信号类型与综合评分完全未变 —— 因此**历史准确率统计不受影响，新记录可与旧
  记录直接比较**，不需要按版本分组
- **覆盖**：新增 `tests/signals.test.js`，逐分断言三档 × 0~100 的仓位提示与信号标签一致，
  并回归上面那两个具体分值

## 1.0.0（2026-09-23）基线

这是引入版本号时的算法状态，**不是一次改动**，而是后续比较的参照物。

### 五因子权重（`js/app.js` `updateSignal`）

| 因子 | 加密币 | 美股 | 缺失时 |
|---|---|---|---|
| 技术面 | 40 | 40 | — |
| 量能 | 20 | 20 | — |
| 市场情绪 | 16 | 0 | 美股不适用，剔除后重新归一 |
| 消息面 | 12 | 0 | 无新闻时按 0 计并重新归一 |
| 衍生品 | 12 | 0 | 取不到资金费率时按 0 计并重新归一 |

`综合分 = Σ(因子得分 × 权重) / Σ权重`。

### 技术面子因子权重（`js/app.js` `sensitivityPresets`）

| 子因子 | 保守 | 均衡（默认） | 灵敏 |
|---|---|---|---|
| macdPos | 6 | 5 | 4 |
| macdHist | 5 | 5 | 5 |
| maCross | 6 | 4 | 3 |
| priceMa25 | 5 | 3 | 2 |
| maFast | 4 | 7 | 9 |
| momentum | 5 | 8 | 10 |
| kdj | 6 | 6 | 7 |
| stoch | 3 | 5 | 7 |
| rsiScale | 0.9 | 0.8 | 0.7 |
| boll | 5 | 4 | 3 |
| volume | 6 | 6 | 6 |

### 信号阈值

| 档位 | 强烈买入 | 买入 | 卖出 | 强烈卖出 |
|---|---|---|---|---|
| 保守 | 74 | 63 | 37 | 26 |
| 均衡（默认） | 70 | 58 | 42 | 30 |
| 灵敏 | 66 | 54 | 46 | 34 |

### 复盘口径（`js/prediction.js`）

- 复盘窗口：当前周期的 5 根K线；下限 10 分钟，上限 14 天
- 方向判定：看多需涨幅 > 0.3%；看空需跌幅 > 0.3%
- 观望判定：|涨跌幅| ≤ 2% 才算判对
- 复盘价格：窗口到期后第一根K线的**收盘价**

## 追溯登记：版本号引入之前的改动

以下改动发生在 2026-09-22 ~ 09-23，当时还没有版本号机制，**无法按版本区分**。
它们都落在引入版本号之前，与 1.0.0 基线视为同一批。

### 1. 修正 `buildROC` 的未来函数

- **位置**：`js/app.js` `buildROC`（`roc` 因子）
- **问题**：`scale` 用最后 50 根K线算出的尺度被套用到整段历史，等于让早期K线用到了当时
  还不存在的未来数据。这会让回测结果偏乐观。
- **改法**：改为逐根走查（`scaleSeries`），每根只用它之前的数据
- **影响**：1h 命中率虚高约 **1.81pp**、4h 约 **0.51pp** 被挤掉；`roc` 子因子的读数随之
  变化，**修正前后的历史准确率数字不可直接比较**

### 2. 预测记录增加币种校验

- **位置**：`js/app.js` `trackPrediction`
- **问题**：`coinInfo` 可能是上一个币种的（价格请求挂住时，那条清理 `coinInfo` 的防线不会
  触发），于是用旧币种的价格记了一条新币种的预测
- **改法**：加 `coinInfo.id === currentCoin` 校验
- **影响**：不改变信号本身，但**修掉的是被污染的数据** —— 修正前后的准确率统计不可比较

### 3. 记录内容扩充（本次）

- **位置**：`js/prediction.js` `record`；`js/app.js` `updateSignal`、`trackPrediction`、`snapshotIndicators`
- **改法**：记录新增三个字段
  - `algoVersion`：产生这条记录的算法版本
  - `factors`：因子快照（五因子得分、当时的权重、技术面子因子拆分、RSI/MACD/KDJ/均线读数、量能指标、费率闸门状态）
  - `criteria`：当时的复盘口径（方向阈值、观望阈值、复盘窗口）
- **影响**：不改变信号结果，但**只有 1.0.0 之后的新记录才带因子快照**
- **⚠️ 分析注意**：本功能上线前产生的 **63 条旧记录没有因子列**。
  按因子分组算均值时必须先把它们排除，否则空值会把分组结果带偏。
  用表格里的「算法版本」列筛掉空值即可。

## 已知但尚未处理的因子问题

留在这里，避免下次分析时重新发现一遍。

- **费率闸门对所有周期生效，但研究结论显示只有 1h 成立。**
  也就是说 4h / 24h 上可能出现「信号成立却被闸门拦下」。这类情况会体现在记录的
  `gate` 与 `fundingPercentile` 字段里 —— 调整闸门之前，先看这两个字段的实际分布，
  确认它到底拦掉了多少信号、拦掉的那些事后是对是错。
- **三个档位的实测命中率都略低于 50%**（见 `js/app.js` 的 `statsByTf`）。
  也就是说当前因子组合在样本内还没有产生正向优势。所以因子分析的目标应先定为
  「找出有区分度的因子」，而不是直接调阈值 —— 在无效的因子上调阈值只会得到
  更精细的噪声。
